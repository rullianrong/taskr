<p id="notice"><%= notice %></p>

<h1>
  <%= @category.title %>
</h1>
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Categories', categories_path %>

<% @category.todos.each do |todo| %>
  <%= render 'tasks/task', task: todo.task %>
<% end %>

<div>
    <%= link_to 'Back to tasks', tasks_path %>
</div>