<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> container content-lgn d-flex justify-content-center">
    <ul class="d-flex flex-column">
      <li>
        <%= value %>
      </li>
    </ul>
  </div>
<% end %>